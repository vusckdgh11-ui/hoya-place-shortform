"use client";

import { ChangeEvent, useEffect, useMemo, useState } from "react";
import { ArrowRight, Check, ChevronRight, Download, ImagePlus, Music2, Play, Sparkles, Store, WandSparkles } from "lucide-react";

const scenes = [
  { time: "0:00–0:03", title: "오늘, 찜닭이 당긴다면", type: "후킹" },
  { time: "0:03–0:08", title: "100% 닭육수로 깊게 끓인 명도찜닭", type: "소개" },
  { time: "0:08–0:14", title: "윤기 나는 당면과 꽉 찬 순살", type: "메뉴" },
  { time: "0:14–0:20", title: "‘양도 넉넉하고 맛있어요’", type: "리뷰" },
  { time: "0:20–0:27", title: "신촌에서 만나는 든든한 한 끼", type: "마무리" },
];

export default function Home() {
  const [storeName, setStoreName] = useState("명도찜닭 마포점");
  const [started, setStarted] = useState(false);
  const [active, setActive] = useState(0);
  const [images, setImages] = useState<string[]>([]);
  const [style, setStyle] = useState("맛집 인플루언서");
  const image = useMemo(() => images[active % images.length], [active, images]);

  useEffect(() => {
    const context = document.modelContext;
    if (!context?.registerTool) return;
    const lifecycle = new AbortController();
    void Promise.resolve(context.registerTool({
      name: "start_shortform_project",
      title: "숏폼 프로젝트 시작",
      description: "가게명을 입력해 해당 가게의 홍보 숏폼 작업 화면을 엽니다.",
      inputSchema: { type: "object", properties: { storeName: { type: "string" } }, required: ["storeName"], additionalProperties: false },
      annotations: { readOnlyHint: false, untrustedContentHint: false },
      execute(input) {
        const name = typeof input === "object" && input && "storeName" in input ? String(input.storeName).trim() : "";
        if (!name) throw new Error("가게명이 필요합니다.");
        setStoreName(name); setStarted(true); return { storeName: name, status: "started" };
      },
    }, { signal: lifecycle.signal })).catch(() => undefined);
    return () => lifecycle.abort();
  }, []);

  function addImages(event: ChangeEvent<HTMLInputElement>) {
    const files = Array.from(event.target.files ?? []);
    setImages((old) => [...old, ...files.map((file) => URL.createObjectURL(file))].slice(0, 30));
  }
  function saveProject() {
    const blob = new Blob([JSON.stringify({ storeName, style, scenes, savedAt: new Date().toISOString() }, null, 2)], { type: "application/json" });
    const url = URL.createObjectURL(blob); const a = document.createElement("a");
    a.href = url; a.download = `${storeName}-shortform-project.json`; a.click(); URL.revokeObjectURL(url);
  }

  return <main className="min-h-screen bg-[#0b0d13] text-white">
    <header className="sticky top-0 z-20 border-b border-white/8 bg-[#0b0d13]/90 px-5 backdrop-blur-xl sm:px-9"><div className="mx-auto flex h-16 max-w-[1500px] items-center justify-between"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-gradient-to-br from-violet-400 to-fuchsia-500 shadow-[0_0_28px_rgba(168,85,247,.5)]"><WandSparkles size={19} strokeWidth={2.5} /></div><div><p className="text-sm font-bold tracking-tight">HOYA Shortform</p><p className="text-[11px] text-white/45">내 가게 홍보영상 작업실</p></div></div><div className="hidden items-center gap-2 text-xs text-white/45 sm:flex"><span className="h-2 w-2 rounded-full bg-emerald-400" /> 개인 작업 모드</div></div></header>
    {!started ? <section className="mx-auto grid min-h-[calc(100vh-64px)] max-w-[1500px] items-center gap-12 px-5 py-12 lg:grid-cols-[1.05fr_.95fr] lg:px-9"><div className="max-w-2xl"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-violet-300/15 bg-violet-400/10 px-3 py-1.5 text-xs font-semibold text-violet-200"><Sparkles size={14} /> 가게 사진으로 30초 홍보영상 만들기</div><h1 className="text-4xl font-black leading-[1.08] tracking-[-0.055em] sm:text-6xl">가게 하나로,<br /><span className="bg-gradient-to-r from-violet-300 via-fuchsia-300 to-orange-200 bg-clip-text text-transparent">바로 쓸 숏폼 한 편.</span></h1><p className="mt-6 max-w-lg text-base leading-7 text-white/55 sm:text-lg">가게명과 사진만 준비하면 대본, 장면 순서, 자막과 성우 구성까지 한 번에 잡는다.</p><div className="mt-9 rounded-2xl border border-white/10 bg-white/[.055] p-3 shadow-2xl shadow-black/20"><div className="flex flex-col gap-2 sm:flex-row"><div className="flex flex-1 items-center gap-3 rounded-xl bg-black/25 px-4"><Store size={18} className="text-violet-300" /><input value={storeName} onChange={(e) => setStoreName(e.target.value)} onKeyDown={(e) => e.key === "Enter" && storeName.trim() && setStarted(true)} className="h-14 w-full bg-transparent text-base font-medium outline-none placeholder:text-white/30" placeholder="예: 명도찜닭 마포점" /></div><button onClick={() => storeName.trim() && setStarted(true)} className="flex h-14 items-center justify-center gap-2 rounded-xl bg-white px-6 font-bold text-[#11121a] transition hover:bg-violet-100">작업 시작 <ArrowRight size={18} /></button></div></div><p className="mt-3 text-xs text-white/35">내 가게 사진을 올려 쓰는 개인용 도구 · 로그인과 결제 없음</p></div><div className="relative mx-auto w-full max-w-[500px]"><div className="absolute -left-12 top-12 h-48 w-48 rounded-full bg-fuchsia-500/20 blur-[80px]" /><div className="absolute -right-8 bottom-10 h-48 w-48 rounded-full bg-orange-400/15 blur-[80px]" /><div className="relative overflow-hidden rounded-[32px] border border-white/15 bg-gradient-to-b from-[#252735] to-[#12141d] p-4 shadow-2xl shadow-black/40"><div className="mb-3 flex items-center justify-between text-xs text-white/55"><span>최종 영상</span><span>9:16 · 0:27</span></div><div className="relative aspect-[9/14] overflow-hidden rounded-2xl bg-[radial-gradient(circle_at_50%_20%,#f6b96d_0,#9e3e20_35%,#1e1620_78%)]"><div className="absolute inset-0 bg-[linear-gradient(135deg,transparent_20%,rgba(255,255,255,.16)_48%,transparent_50%)]" /><div className="absolute inset-x-5 bottom-7"><p className="text-2xl font-black leading-tight tracking-[-.04em] drop-shadow-lg">오늘 찜닭이<br />당긴다면</p><div className="mt-3 h-1 w-16 rounded-full bg-violet-300" /></div><div className="absolute left-4 top-4 rounded-full bg-black/30 px-2.5 py-1 text-[10px] font-semibold">명도찜닭 마포점</div></div><div className="mt-4 flex gap-2"><span className="h-1.5 flex-1 rounded-full bg-violet-400" /><span className="h-1.5 flex-1 rounded-full bg-white/15" /><span className="h-1.5 flex-1 rounded-full bg-white/15" /></div></div></div></section> : <section className="mx-auto max-w-[1500px] px-5 py-6 sm:px-9"><div className="mb-6 flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-semibold tracking-[.13em] text-violet-300">PROJECT 01</p><h1 className="mt-1 text-2xl font-black tracking-tight">{storeName} <span className="font-medium text-white/40">· 홍보 숏폼</span></h1></div><button onClick={saveProject} className="flex items-center gap-2 rounded-xl border border-white/12 bg-white/5 px-4 py-2.5 text-sm font-semibold hover:bg-white/10"><Download size={16} /> 프로젝트 저장</button></div><div className="grid gap-5 xl:grid-cols-[310px_minmax(0,1fr)_360px]"><aside className="rounded-2xl border border-white/10 bg-white/[.035] p-4"><p className="mb-3 text-xs font-bold tracking-wider text-white/50">1. 사진 불러오기</p><label className="flex min-h-36 cursor-pointer flex-col items-center justify-center rounded-xl border border-dashed border-white/20 bg-black/15 p-4 text-center transition hover:border-violet-300/60 hover:bg-violet-400/5"><ImagePlus size={22} className="mb-2 text-violet-300" /><span className="text-sm font-semibold">사진 선택</span><span className="mt-1 text-xs text-white/40">음식 · 매장 · 메뉴판</span><input type="file" accept="image/*" multiple className="hidden" onChange={addImages} /></label><div className="mt-4 grid grid-cols-3 gap-2">{images.map((src, i) => <img key={src} src={src} alt={`업로드 사진 ${i + 1}`} className="aspect-square rounded-lg object-cover" />)}{!images.length && [0,1,2,3,4,5].map((i) => <div key={i} className="aspect-square rounded-lg bg-gradient-to-br from-white/10 to-white/[.025]" />)}</div><p className="mt-3 text-xs leading-5 text-white/35">사진을 10장 이상 넣으면 장면 자동 선택 품질이 좋아진다.</p></aside><div className="rounded-2xl border border-white/10 bg-[#13151e] p-4 sm:p-6"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs font-bold tracking-wider text-white/50">2. 영상 미리보기</p><p className="mt-1 text-sm text-white/55">장면을 눌러 자막과 사진을 바꿀 수 있어.</p></div><span className="rounded-lg bg-violet-400/10 px-2.5 py-1 text-xs font-bold text-violet-200">0:27</span></div><div className="mx-auto flex max-w-[420px] justify-center"><div className="relative aspect-[9/14] w-full max-w-[300px] overflow-hidden rounded-[24px] bg-gradient-to-br from-[#7d3523] via-[#1c1820] to-[#0c0d13] shadow-[0_18px_60px_rgba(0,0,0,.5)]">{image ? <img src={image} alt="선택 장면" className="h-full w-full object-cover opacity-90" /> : <div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_15%,rgba(253,190,104,.92),transparent_31%),linear-gradient(160deg,#512419,#15131b_64%)]" />}<div className="absolute inset-0 bg-gradient-to-t from-black/75 via-transparent to-black/15" /><span className="absolute left-4 top-4 rounded-full bg-black/40 px-2.5 py-1 text-[10px] font-semibold">{storeName}</span><div className="absolute inset-x-5 bottom-8"><p className="text-[28px] font-black leading-[1.08] tracking-[-.055em] drop-shadow-xl">{scenes[active].title}</p><div className="mt-3 h-1.5 w-16 rounded-full bg-violet-300" /></div><button className="absolute left-1/2 top-1/2 grid h-12 w-12 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full bg-white/90 text-[#11121a] shadow-xl"><Play size={18} fill="currentColor" /></button></div></div><div className="mt-5 flex items-center gap-1.5">{scenes.map((scene, i) => <button key={scene.time} onClick={() => setActive(i)} className={`h-2 flex-1 rounded-full transition ${active === i ? "bg-violet-400" : "bg-white/12 hover:bg-white/25"}`} aria-label={`${i + 1}번 장면`} />)}</div></div><aside className="rounded-2xl border border-white/10 bg-white/[.035] p-4"><p className="mb-3 text-xs font-bold tracking-wider text-white/50">3. 장면 구성</p><div className="space-y-2">{scenes.map((scene, i) => <button key={scene.time} onClick={() => setActive(i)} className={`w-full rounded-xl border p-3 text-left transition ${active === i ? "border-violet-400/70 bg-violet-400/10" : "border-transparent bg-black/15 hover:border-white/10"}`}><div className="flex items-center justify-between text-[11px]"><span className="font-semibold text-violet-200">{scene.type}</span><span className="text-white/35">{scene.time}</span></div><p className="mt-1 text-sm font-semibold leading-5">{scene.title}</p></button>)}</div><div className="mt-4 border-t border-white/10 pt-4"><p className="mb-2 text-xs font-bold tracking-wider text-white/50">영상 스타일</p><div className="flex flex-wrap gap-2">{["맛집 인플루언서", "깔끔한 메뉴 소개", "후기 강조"].map((item) => <button key={item} onClick={() => setStyle(item)} className={`rounded-lg px-2.5 py-2 text-xs font-semibold ${style === item ? "bg-white text-black" : "bg-white/8 text-white/55 hover:bg-white/15"}`}>{item}</button>)}</div></div><button className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-violet-500 to-fuchsia-500 py-3 text-sm font-bold shadow-lg shadow-violet-500/20"><Music2 size={16} /> 대본·성우 생성 준비 <ChevronRight size={16} /></button></aside></div><div className="mt-5 flex items-center gap-2 rounded-xl border border-emerald-400/15 bg-emerald-400/[.06] px-4 py-3 text-sm text-emerald-100/80"><Check size={16} className="text-emerald-300" /> 현재는 내 사진과 영상 구성을 먼저 잡는 버전이야. 다음 단계에서 AI 대본·성우·MP4 렌더링을 연결한다.</div></section>}
  </main>;
}
